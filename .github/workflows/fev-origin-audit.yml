name: FEV origin audit

on:
  push:
    branches:
      - tmp/fev-origin-audit
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install numpy pandas scipy scikit-learn datasets pyarrow huggingface_hub
      - name: Run exact-FEV GFC14 origin audit
        run: |
          python experiments/fev_origin_audit.py | tee fev_origin_audit.log
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fev-origin-audit-results
          path: |
            tmp_fev_audit_results/
            fev_origin_audit.log
          if-no-files-found: warn
